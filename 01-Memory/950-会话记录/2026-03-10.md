## 会话纪要补充（技能治理 / SOUL 补丁 / 模型配置）

- 今天系统梳理了技能库冲突问题，确认 `/root/.openclaw/skills` 与 `/root/.openclaw/workspace/skills` 存在 28 个重复技能；形成方案 B：清重复、定主源、收窄触发词、建立安装/发布规范。
- 明确建议将 `workspace/skills/` 作为源码主仓，`~/.openclaw/skills/` 作为运行入口，优先用软链收口。
- 对 Browser Master 的定位完成收敛：它应是“路由 / 决策 / 升级策略层”，不应成为新的全能执行技能；相关边界、冲突清单与落地实施方案已补入飞书文档。
- 发现并记录 Feishu Doc 工具经验：`append` 在该文档上持续返回 400，改为 `insert` 且插到顶层 block 后面才成功。
- 将“技能治理”提升为“默认行为边界”问题，并以增量补丁方式写入 `SOUL.md`。先备份文件，再只追加一小段，避免重写原人格。
- `SOUL.md` 新增内容为“默认行为铁律”：
  - 默认保守，禁止静默接管。
  - 边界没说明白，就别接管已有场景。
  - 别默认覆盖现有路径；要替代，先写清替代关系。
  - 有歧义时默认暂停，不靠猜测补全执行意图。
- 备份文件位置：`/root/.openclaw/workspace/SOUL.md.bak.20260310081254`。
- 讨论确认：这段规则是补充而非替换，后续观察是否过于保守；如有问题，可直接回滚或微调 wording。
- 查询到当前模型配置核心：默认主模型为 `openai-codex/gpt-5.4`（别名 `gpt54`），fallback 为 `openai-codex/gpt-5.3-codex` 与 `minimax-cn/MiniMax-M2.5`。
- 查询到 `openai-codex/gpt-5.4` 当前额度：5 小时剩余 62%，周额度剩余 43%。
- 澄清 `minimax-cn/MiniMax-M2.5` 与 `minimax-portal/MiniMax-M2.5` 的区别主要在接入方式与 provider 语义，不是完全不同的模型本体：前者更像 API key 直连标准版，后者更像 portal OAuth 接入版。
- 阅读并总结飞书 Wiki 文档《OpenClaw 企业级智能助手部署方案【值得注意】》：核心卖点是企业统一部署、飞书原生可操作、每员工独立容器、IT 可治理；判断其适合作为销售前置方案，不适合直接当安全白皮书。
- 尝试安装 `elite-longterm-memory-v1.2.3` 时，被 ClawHub / VirusTotal Code Insight 标记为 suspicious，非交互安装需 `--force`；未继续强装，结论是这类长期记忆技能最好先审代码再决定。
- 已把今天的工作沉淀成总结文件：`500 Journal/2026-03-10-技能治理与SOUL补丁纪要.md`。
