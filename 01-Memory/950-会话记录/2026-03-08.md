- 2026-03-08: 用户要求优化 weixin-reader 技能（遇到“轻触查看原文”壳页时抓不到正文）。后续改进方向：1) 增加备用抓取策略（移动UA+Referer+重试）2) 自动提取 og/url 并尝试跳转真实页 3) 失败时输出可执行降级指引模板（让用户一键提供正文/截图）。
- 2026-03-08: 完成并推送 `feishu-doc` 技能权限流程优化到 `openclaw-skills`（GitHub/Gitee）。关键经验：创建飞书文档时 `owner_open_id` 必须视为强制项；创建成功不等于权限已生效，需“create -> verify -> compensate”流程，必要时用权限工具补偿。
- 2026-03-08: 误将远程 `openclaw-skills` 仓库缩成仅剩 `feishu-doc`，后已恢复非飞书技能。用户明确要求：以后只改用户明确指定的技能目录，推送前必须检查 `git diff --name-status`，若有非目标路径改动必须中止并汇报。
- 2026-03-08: 新增 Gitee SSH key 并打通 `openclaw-skills` 双远程推送（GitHub + Gitee）。
- 2026-03-08: MiniMax Coding Plan MCP 已接入 OpenClaw：通过 `openclaw-mcp-adapter` 注册 `MiniMax_web_search` 与 `MiniMax_understand_image`，并完成实测。关键坑：MCP adapter server 配置里的 `env` 必须是对象，不是数组；修改配置前后必须先备份、先验证、未经确认不重启，避免 Gateway 起不来导致失联。
- 2026-03-08: 用户强烈强调高风险配置变更流程：先检查、先备份、先准备回滚、只改最小范围、先写配置不重启、得到确认后再重启。此规则应视为硬约束。
- 2026-03-08: 优化并推送 `weixin-reader` 技能：增加多 UA 重试、壳页检测（“轻触查看原文”）、`og:title/og:description` 兜底提取、`--json/--quiet` 模式；已用微信文章链接实测通过。后续可继续增强为更稳定的微信正文提取方案。
- 2026-03-08: 与用户共创“browser-login-automation” 方案，并写入飞书文档 `https://feishu.cn/docx/BTGjd88yAoXMhLxSmoPcfwvtncd`。文档已扩展到 v1.4 封板实施版，覆盖新技能 + 现有技能增强、Ubuntu/无图形服务器适配、验证与发布门禁、账号/会话治理、RACI、回滚 Runbook、SLO、灰度扩容、安全审计、MiniMax MCP 工具与排障等。后续若继续推进浏览器登录态自动化，应以该文档为 SSOT。
- 2026-03-08: 用户对浏览器登录态自动化高度关注；推荐架构为“1个新技能 + 2个现有技能增强 + 可选 stealth 备链路”，并强调登录态复用、风控兜底、失败截图与人工接管。
- 2026-03-08: 用户对 OpenClaw/GPT-5.4 状态持续关注；调查结果：本机当前仍为 `2026.3.2`，而最新稳定版已到 `v2026.3.7`，release note 已包含 GPT-5.4 / openai-codex 相关修复与兼容增强。若后续要升级，必须走低风险升级与回滚流程。
- 2026-03-08: 用户偏好明确：需要直接、结构化、可复制粘贴的产出；大量偏好通过飞书云文档沉淀方案，适合继续用 Feishu doc 作为需求与实施蓝图载体。
